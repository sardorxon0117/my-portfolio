{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Vercel deployment manifest — installs the same dependencies server/server.js needs, since api/index.js requires it from the project root. Local development still uses server/package.json (cd server && npm run dev).",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.1085.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1"
  }
}
